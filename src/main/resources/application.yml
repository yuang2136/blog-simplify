server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/blog_simplify?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

    spring:
      redis:
        host: localhost
        port: 6379
        database: 0

